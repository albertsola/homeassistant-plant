[pytest]
asyncio_mode = auto
testpaths = tests
# The tests import custom_components.plant_care directly, so the repo root has
# to be importable regardless of how pytest is invoked.
pythonpath = .
addopts = -q
